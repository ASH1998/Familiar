{
  "name": "dungeon-familiar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "assets:extract": "python3 tools/unity-extract.py --out build --copy-png",
    "assets:copy": "node scripts/copy-assets.mjs"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "^0.2.0",
    "typescript": "^5.6.3",
    "vite": "^8.2.2",
    "vitest": "^2.1.4"
  }
}
